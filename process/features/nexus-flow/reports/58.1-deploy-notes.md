# Phase 58.1 — Deploy Notes (NEXUS Flow)

Status as of 2026-06-18. Branch `feat/fb-comment-to-message`.

## Pending VPS deployment (not yet live in prod)

- **Migration `0015_phase58_nexus_flows`** — creates `app.nexus_flows` + `app.flow_runs`.
  Down-revision chains to `0014`. `./deploy-rag.sh` runs `alembic upgrade head` and will apply it.
  Round-trip (`upgrade → downgrade -1 → upgrade`) is **unverified locally** (no local Postgres; same
  situation as 0014). Verify on the VPS after deploy.
- **Env flag `NEXUS_FLOWS_ENABLED`** — backend config `nexus_flows_enabled` (default `false`). Set it in
  `/home/nexus-rag/.env` to switch comment dispatch from the Phase 57 keyword engine to the flow engine
  (coexist-with-precedence: active flows match first, else fall back to Phase 57). **Recreate** the
  container (don't restart) to reload env. Until set, behavior is unchanged.

## Frontend

- New dependency **`@xyflow/react` ^12.11.0** added to `nexus-ui/package.json` (+ lockfile). React Flow
  canvas. `FlowBuilderPage` is lazy-loaded (code-split) so the canvas bundle stays out of the main chunk.
- Routes `/flows` (list) and `/flows/:id` (builder) mounted under the `RequireManager` guard in
  `App.jsx`. Manager nav entry added (`MANAGER_NAV` in `lib/nav.js`, `nav.flows` label, en-authoritative).
- i18n namespace `flows` added across all 7 locales (en authoritative; 6 mirror English).

## Known limitations / follow-ups

- Live data calls from local `npm run dev` hit the **prod** backend (vite proxy is hardcoded — ignores
  `VITE_API_TARGET`). Until 0015 + the flag are deployed, `/api/tenants/{id}/facebook/flows` returns
  4xx in dev; the canvas itself still renders.
- Backend commit: `4bb6224` (`feat(flows): phase 58.1 backend — schema, traversal engine, CRUD API`).
- Remaining program phases (58.2 AI Router + Pause, 58.3 CRM + Webhook, 58.4 Story Mention) are not in
  this slice — see `active/nexus-flow-58.1_PLAN_18-06-26.md`.
