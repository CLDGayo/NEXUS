import Placeholder from './Placeholder.jsx';
export default function SettingsPage() {
  return <Placeholder name="Settings" description="Tunable env + password rotate + JWT rotate — port pending." />;
}
