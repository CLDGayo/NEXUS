"""Service-layer modules — thin wrappers around external systems.

Phase 28 Part 2 introduces this package with ``object_store`` (Minio / S3)
as its first occupant. Future home for any client that needs a single point
of construction, retry policy, and test seam.
"""
